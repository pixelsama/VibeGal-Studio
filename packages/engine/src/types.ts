/**
 * Backwards-compatible type entry point.
 *
 * Structural project types are inferred in @vibegal/contracts and re-exported
 * here to preserve the existing @vibegal/engine API.
 */
export type * from "@vibegal/contracts/types";
