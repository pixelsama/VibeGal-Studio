This directory receives the precompiled `vibegal-player-tauri` binary during
`tauri build`. The bundled CLI reuses that player for lightweight desktop game
exports, so individual games never compile Rust code.
